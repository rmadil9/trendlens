# Web framework
fastapi==0.115.6
uvicorn[standard]==0.29.0

# RSS + HTTP
feedparser==6.0.11
httpx==0.27.0          # async HTTP client for article fetching

# HTML → text
beautifulsoup4==4.12.3

# Text splitting
langchain-text-splitters==0.3.8

# Vector store client
qdrant-client==1.12.2

# Sparse (BM25) embeddings for hybrid search
fastembed==0.8.0

# Cross-encoder reranking
sentence-transformers==5.6.0

# OpenAI (embeddings + generation)
openai==1.30.1

# Retry with exponential backoff for transient OpenAI API failures
tenacity==9.1.4

# Scheduling (post-MVP: swap for RabbitMQ consumer)
apscheduler==3.10.4

# Env management
python-dotenv==1.0.1

# Utilities
pydantic==2.10.6        # data validation, used by FastAPI
pydantic-settings==2.7.0  # load settings from .env
