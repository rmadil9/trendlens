# Web framework
fastapi==0.115.6
uvicorn[standard]==0.29.0

# RSS + HTTP
feedparser==6.0.11
httpx==0.27.0          # async HTTP client for article fetching

# HTML → text
beautifulsoup4==4.12.3

# Text splitting
langchain-text-splitters==0.3.8

# Vector store client
qdrant-client==1.9.1

# OpenAI (embeddings + generation)
openai==1.30.1

# Scheduling (post-MVP: swap for RabbitMQ consumer)
apscheduler==3.10.4

# Env management
python-dotenv==1.0.1

# Utilities
pydantic==2.10.6        # data validation, used by FastAPI
pydantic-settings==2.7.0  # load settings from .env
