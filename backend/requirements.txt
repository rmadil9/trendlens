# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# RSS + HTTP
feedparser==6.0.11
httpx==0.27.0          # async HTTP client for article fetching

# HTML → text
beautifulsoup4==4.12.3
lxml==5.2.2            # faster BS4 parser

# Vector store client
qdrant-client==1.9.1

# OpenAI (embeddings + generation)
openai==1.30.1

# Scheduling (post-MVP: swap for RabbitMQ consumer)
apscheduler==3.10.4

# Env management
python-dotenv==1.0.1

# Utilities
pydantic==2.7.1        # data validation, used by FastAPI
pydantic-settings==2.2.1  # load settings from .env
